// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); // redondea .4 hacia abajo
resultado = Math.floor(2.6); // Floor siempre redondea hacia abajo
resultado = Math.ceil(2.6); // Ceil siempre redondea hacia arriba
resultado = Math.sqrt(200); // Raiz cuadrada
resultado = Math.abs(-200); // Absoluto
resultado = Math.min( 3, 5, 1, 8, 2, 10 ); // mínimo entre los números
resultado = Math.max( 3, 5, 1, 8, 2, 10 ); // máximo entre los números
resultado = Math.random(); // número aleatorio
resultado = Math.floor(Math.random() * 30); // ejemplo de salida de datos


console.log(resultado);